#include "../headers/parse.h"



int fill_lst(char *str, t_parse_lst *pars_lst)
{
    t_deviders *dev_lst;

    dev_lst = get_deviders_list(str);
    while (dev_lst)
    {
        
    }
    return (1);
}